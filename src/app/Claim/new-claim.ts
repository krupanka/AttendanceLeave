export interface NewClaim {
    id: string,
    employee: 4,
     claim_type: string,
     description : string,
      amount: number,
       date : Date,
        document: File,
        status: string,
}
